# 心事邮局 — 产品经理技术扫盲手册

> **目标**：让你在与开发团队沟通时，能听懂技术术语、问出关键问题、做出合理决策，而不是被技术名词挡在门外。

---

## 目录

1. [阅读指南：PM 需要"在行"到什么地步](#阅读指南)
2. [你真的需要理解"计算机是怎么运行的"](#一你真的需要理解计算机是怎么运行的)
3. [前端：用户看到的那个"壳"](#二前端用户看到的那个壳)
4. [后端：用户看不到的那个"大脑"](#三后端用户看不到的那个大脑)
5. [数据库：数据存在哪里](#四数据库数据存在哪里)
6. [前后端之间：它们怎么"说话"](#五前后端之间它们怎么说话)
7. [安全与权限：谁能看什么、做什么](#六安全与权限谁能看什么做什么)
8. [开发流程与工具链](#七开发流程与工具链)
9. [核心技术概念速查表](#八核心技术概念速查表)
10. [PM 必备话术：如何用技术语言描述需求](#九pm-必备话术如何用技术语言描述需求)
11. [常见需求的技术代价评估](#十常见需求的技术代价评估)

---

## 阅读指南

### PM 需要"在行"到什么地步？

一句话总结：**你不需要会写代码，但你需要能"翻译"——把用户需求翻译成技术团队能理解的语言，把技术约束翻译成业务团队能接受的方案。**

具体来说，你应该能做到：

| 层级 | 要求 | 示例 |
|------|------|------|
| **听懂** | 开发说"这个字段需要加个索引"，你知道他在说什么 | "索引"≈书的目录，让查找变快 |
| **问对** | 你能针对方案提出关键问题 | "这个改动的数据迁移方案是什么？会影响多少存量用户？" |
| **判断** | 你能评估需求的实现难度 | "加个导出按钮"和"改成实时协同编辑"，你知道后者耗费巨大 |
| **翻译** | 你能把模糊需求落地为技术可执行方案 | "用户想回顾心情" → "需要情绪统计接口，按周/月维度聚合" |
| **避险** | 你能识别技术方案中的风险 | "把密码明文存日志？这不行，必须脱敏" |

下面的内容，以你能达到以上 5 条为标准来写 —— 不深究实现细节，但覆盖你需要知道的所有概念。

---

## 一、你真的需要理解"计算机是怎么运行的"

### 1.1 客户端 vs 服务器

这是最基础的二分法，几乎所有技术决策的起点。

```
     用户设备（客户端）                    远程机器（服务器）
  ┌──────────────────────┐         ┌──────────────────────┐
  │  浏览器/手机App      │  ←→    │  Node.js + MongoDB    │
  │  （前端代码）         │  网络   │  （后端代码 + 数据库）  │
  └──────────────────────┘         └──────────────────────┘
```

| 概念 | 白话解释 | 本项目对应 |
|------|----------|------------|
| **客户端 (Client)** | 用户手里的设备，运行前端代码 | `client/` 目录，Vue 写的网页 |
| **服务器 (Server)** | 云端/机房里的机器，运行后端代码 | `server/` 目录，Express.js |
| **数据库 (Database)** | 专门存数据的地方，也在服务器上 | MongoDB，数据持久化 |

**PM 应该问的**："这个功能计算量大，放客户端做还是放服务端做？"——这决定了用户手机发不发热、加载快不快。

### 1.2 浏览器是怎么渲染一个页面的

用户打开你的网页，经历了这些步骤：

```
1. 输入网址 → 2. DNS解析（域名→IP地址） 
→ 3. 浏览器发HTTP请求 → 4. 服务器返回HTML文件
→ 5. 浏览器解析HTML → 6. 加载CSS（样式）和JS（交互逻辑）
→ 7. Vue接管页面 → 8. 渲染出最终界面
```

**你不需要记住每一步，但你需要知道**：
- 如果用户说"打开很慢"，可能卡在第3步（网络慢）、第5步（文件太大）、或第7步（前端代码效率低）
- CSS 管"长什么样"，JS 管"点了之后发生什么"

### 1.3 前端和后端的职责边界

这是一张你需要刻在脑子里的表：

| 关注点 | 前端负责 | 后端负责 |
|--------|----------|----------|
| **界面展示** | ✅ 按钮长什么样、颜色、动画 | ❌ |
| **交互反馈** | ✅ 点了按钮弹什么提示 | ❌ |
| **数据校验** | 可做初步校验（如空值检查） | ✅ 最终权威校验 |
| **权限判断** | 可做展示级判断（隐藏按钮） | ✅ 最终权威判断 |
| **数据存储** | ❌ 不能信任客户端 | ✅ 所有数据最终存在这里 |
| **业务逻辑** | ❌ 不能放核心逻辑 | ✅ 计算、统计、审核等 |

**关键认知**：前端的一切都可以被篡改。你在前端"隐藏"了一个管理员按钮，懂技术的人F12就能看到。真正的安全防线永远在后端。

---

## 二、前端：用户看到的那个"壳"

### 2.1 本项目的技术栈

```
Vue 3           ← 前端框架，决定代码怎么组织
  ├── Vite      ← 构建工具，把代码打包成浏览器能看懂的
  ├── Vue Router ← 管理页面跳转（URL → 哪个页面）
  ├── Pinia     ← 状态管理（用户信息、数据缓存）
  ├── TipTap    ← 富文本编辑器（就是那个能加粗、斜体的输入框）
  ├── Element Plus ← UI组件库（按钮、输入框、弹窗等现成组件）
  ├── Axios     ← 发HTTP请求的工具（前端跟后端"说话"的管道）
  └── SCSS      ← CSS的增强版（写样式的）
```

### 2.2 Vue 3：组件化思维

**核心概念：一切皆组件。**

```
一个网页 = 组件树

App.vue (根)
├── DustParticles.vue (飘尘粒子)
├── NavBar.vue (导航栏)
└── <router-view> (当前页面)
    └── HomePage.vue (首页)
        ├── 心情分布卡片
        ├── 呼吸引导卡片
        ├── 冥想引导卡片
        └── 推荐树洞卡片
```

**PM 应该懂的**：
- 开发说"抽一个公共组件"——就是把多个页面重复用的部分提取出来，改一处全站生效
- 开发说"这个组件耦合太紧"——这组件跟别的组件绑死了，不好复用
- 开发说"用 Composition API 重写"——Vue 3 的新写法，代码更清晰（对你来说是透明的）
- 开发说"Props 传参"——父组件给子组件传数据，就像你把信纸内容传给"信纸卡片组件"
- 开发说"Emit 事件"——子组件通知父组件"我被点了"，就像信纸卡片点击后告诉页面"这个人点了我"

**你需要知道文件怎么对应页面**：

| 文件 | 页面 | 路由 |
|------|------|------|
| `views/HomePage.vue` | 门厅首页 | `/` |
| `views/RecycleStation.vue` | 心事寄存处 | `/deposit` |
| `views/TreeholePage.vue` | 回声广场 | `/plaza` |
| `views/ArticlesPage.vue` | 夜灯书房 | `/study` |
| `views/AdminPage.vue` | 审核中心 | `/admin` |
| ... | ... | ... |

### 2.3 Vue Router：页面跳转

**核心概念**：URL 和页面组件的映射表。

```
用户访问 /plaza → Router 查找路由表 → 加载 TreeholePage.vue → 渲染
```

**PM 应该懂的**：

| 术语 | 白话 | 本项目示例 |
|------|------|------------|
| **路由** | URL路径和页面的对应关系 | `/deposit/attic` → `AtticPage` |
| **动态路由** | URL里带参数的路径 | `/plaza/:id` → `treeholeDetail`（:id 是具体的一条树洞ID） |
| **路由守卫** | 进页面前的检查 | 没登录的人访问 `/deposit` 会被自动跳转到登录页 |
| **懒加载** | 用到才加载，首页不加载所有页面代码 | `() => import('@/views/AdminPage.vue')` |

**PM 应该问的**："加这个新页面，路由守卫怎么配？需要登录吗？需要管理员权限吗？"

### 2.4 Pinia：状态管理

**为什么需要它？** 多个页面需要共享同一份数据。

```
场景：用户登录后，NavBar 要显示头像，个人中心要显示昵称，
      设置页要改昵称，改完 NavBar 也要变。

不用Pinia：每个页面各自存一份用户数据 → 改了不同步 → 显示不一致
用Pinia：  所有页面读同一份"中央数据" → 改一处全站更新
```

**本项目有3个 Store**：

| Store | 存什么 | 关键数据 |
|-------|--------|----------|
| `useUserStore` | 用户信息 | user对象、token、isLoggedIn、isAdmin |
| `useNoteStore` | 便签数据 | notes列表、增删改查方法 |
| `useTreeholeStore` | 树洞数据 | treeholes列表、评论、详情 |

**PM 该懂的**：开发说"状态提升到 Store"——就是把原来只在 A 页面的数据，放到全局 Store 里，让 B 页面也能用。

### 2.5 TipTap：富文本编辑器

**这是什么**：就是你能在输入框里加粗、斜体、标题、列表、引用块的那个编辑器。

**为什么用它**：用户不是只能输纯文本，可以像写 Word 一样排版。

**PM 该懂的**：
- 它输出的是 **HTML 字符串**（不是纯文本），后端存的也是 HTML
- 前端展示时用 `v-html` 渲染这个 HTML 字符串
- 工具栏按钮（B/I/U/H1/H2/ul/ol/引用/代码块）是你和开发可以协商增减的

### 2.6 Element Plus：UI组件库

**这是什么**：一套现成的 UI 组件（按钮、输入框、弹窗、开关、下拉选择等），不用从零画。

**PM 该懂的**：
- 开发说"用 el-dialog 包一下"——就是用 Element Plus 的弹窗组件
- 开发说"用 el-input"——就是用 Element Plus 的输入框（带 placeholder、show-word-limit 等内置功能）
- 好处：样式统一、开发快；坏处：定制样式时可能需要"覆盖"组件库默认样式

### 2.7 Axios：前端和后端的"电话线"

```
前端需要数据 → Axios 发 HTTP 请求 → 后端处理 → 返回 JSON → Axios 收到
```

**本项目约定**：
- 所有请求前缀 `/api`（如 `/api/treehole`、`/api/auth/login`）
- 需要登录的请求自动在 Header 里带 `Authorization: Bearer <token>`
- 响应格式统一为 `{ code: 200, message: "...", data: {...} }`

**PM 该懂的**：
- 开发说"调接口"——就是 axios 发请求到后端
- 开发说"接口挂了/500了"——后端返回了错误
- 开发说"跨域问题"——浏览器安全限制（前端和后端不在同一个域名下），后端已用 `cors()` 解决

---

## 三、后端：用户看不到的那个"大脑"

### 3.1 本项目的技术栈

```
Node.js        ← JavaScript运行时（让JS能在服务器上跑）
  └── Express.js ← Web框架（处理HTTP请求，定义路由）
       ├── mongoose ← 操作MongoDB的工具
       ├── jsonwebtoken ← 生成和验证JWT
       ├── bcryptjs ← 密码加密
       ├── cors ← 解决跨域
       ├── dotenv ← 读取.env环境变量
       └── express-rate-limit ← 限流
```

### 3.2 Express.js：请求-响应模型

**核心概念**：后端就是一台"自动答录机"。

```
客户端发请求 → Express 接收 → 匹配路由 → 执行业务逻辑 → 返回响应
```

**本项目路由结构**：

```
POST /api/auth/login      → routes/auth.js      → 登录逻辑
GET  /api/notes           → routes/notes.js     → 查便签
POST /api/treehole        → routes/treehole.js  → 发树洞
GET  /api/articles/:id    → routes/articles.js  → 看文章详情
PUT  /api/users/admin/treeholes/:id/review → routes/users.js → 审核树洞
```

**PM 该懂的——HTTP方法**：

| 方法 | 语义 | 示例 |
|------|------|------|
| `GET` | 查 | 获取文章列表、获取用户信息 |
| `POST` | 增 | 注册、发布树洞、创建便签 |
| `PUT` | 改 | 编辑便签、审核通过/驳回 |
| `DELETE` | 删 | 删除便签、删除账号 |

**PM 该懂的——状态码**：

| 状态码 | 含义 | 本项目的使用场景 |
|--------|------|------------------|
| `200` | 成功 | 正常返回数据 |
| `201` | 创建成功 | 注册成功、便签创建 |
| `400` | 客户端错误（参数不对） | 密码太短、内容为空 |
| `401` | 没登录/登录过期 | Token失效 |
| `403` | 没权限 | 普通用户访问管理接口 |
| `404` | 找不到 | 文章/树洞不存在 |
| `429` | 请求太频繁 | 触发限流 |
| `500` | 服务器内部错误 | 代码报错、数据库挂了 |

### 3.3 中间件：请求的"安检通道"

```
请求进来 → [cors检查] → [JSON解析] → [限流检查] → [JWT认证] → [业务逻辑]
           ↑ 中间件1    ↑ 中间件2     ↑ 中间件3    ↑ 中间件4
```

**本项目3个关键中间件**：

| 中间件 | 文件 | 作用 |
|--------|------|------|
| `auth` | `middleware/auth.js` | 强制要求登录，从Header提取JWT，查数据库验证用户 |
| `optionalAuth` | `middleware/auth.js` | 有Token就解析用户，没有也不拦（游客也可以看） |
| `adminAuth` | `middleware/auth.js` | 检查 `user.role === 'admin'`，不是管理员就拒绝 |

**PM 该懂的**："这个接口需要加 auth 中间件"——就是要登录才能用。

### 3.4 Mongoose：操作 MongoDB 的"翻译官"

MongoDB 是 NoSQL 数据库（后面细讲），Mongoose 是 JS 代码和 MongoDB 之间的桥梁。

**核心概念：Schema = 数据表的"蓝图"**

```javascript
// User 的 Schema 定义了用户数据长什么样
userSchema = {
  username: { type: String, required: true, minlength: 3, maxlength: 20 },
  password: { type: String, required: true, minlength: 6 },
  role: { type: String, enum: ['user', 'admin'] },
  ...
}
// 这意味着：
// - username 必须是3-20个字符
// - password 至少6位
// - role 只能是 'user' 或 'admin'
```

**PM 该懂的——校验发生在哪里**：

| 层次 | 位置 | 能不能被绕过 |
|------|------|-------------|
| 前端校验 | Vue 组件里的 `v-if`、`disabled` | ✅ 可以被绕过（用户F12改代码） |
| Mongoose Schema校验 | 后端 Model | ❌ 不能被绕过（最终防线） |
| 路由层逻辑校验 | 后端 Route handler | ❌ 不能被绕过 |

**PM 应该问的**："这个字段的校验规则，在前端做了、Schema里也定义了吗？两边的规则一致吗？"

### 3.5 JWT：Token 认证是怎么工作的

**为什么不用"登录后服务器记住你"的传统方式？**

传统 Session：服务器要存"谁登录了"的信息 → 用户多了内存爆 → 多台服务器要共享 Session → 麻烦。

JWT 方式：服务器给你发一张"加密身份证"，你自己保管，每次请求带着就行。

```
登录流程：
用户输入账号密码 → 后端验证 → 生成JWT Token → 返回给前端 → 前端存 localStorage

后续请求：
前端发请求（Header里带Token）→ 后端解密Token → 确认身份 → 执行操作
```

**Token 里面有什么**？（你用不到，但知道就行）
```
Header:  { "alg": "HS256" }           ← 加密算法
Payload: { "userId": "abc123", ... }  ← 用户ID，过期时间等
Signature: "xxxx"                     ← 用密钥生成的签名
```

**PM 该懂的**：
- Token 过期时间：本项目设置 `JWT_EXPIRES_IN=7d`，7天后需要重新登录
- Token 存哪里：前端存 `localStorage`，关浏览器也不会丢
- 安全性：Token 用 `JWT_SECRET` 签名，只要密钥不泄露就安全

### 3.6 bcrypt：密码为什么不能明文存

```
用户密码 "123456" 
→ bcrypt 加盐哈希 → "$2a$10$N9qo8uLOickgx2ZMRZoMye..."
→ 存数据库

下次登录：用户输入 "123456"
→ bcrypt 比对 → 匹配成功 ✅

即使数据库泄露，攻击者也看不见原始密码
```

**PM 该懂的**：开发说"密码哈希"、"加盐"、"不可逆加密"——都是说密码不能被还原，安全。

---

## 四、数据库：数据存在哪里

### 4.1 MongoDB vs MySQL（NoSQL vs SQL）

```
MySQL（关系型）              MongoDB（文档型，本项目使用）
┌─────────────────┐         ┌─────────────────────────┐
│ User表            │         │ users collection          │
│ ┌──┬────┬──────┐ │         │ {                         │
│ │id│name│email │ │         │   _id: ObjectId,          │
│ ├──┼────┼──────┤ │         │   username: "zhangsan",    │
│ │1 │张三 │z@x.com│ │         │   likedArticles: [...],   │ ← 数组直接嵌在里面
│ └──┴────┴──────┘ │         │   readingHistory: [...]    │ ← 不用另建表
│                    │         │ }                         │
│ Article表          │         └─────────────────────────┘
│ ┌──┬──────┬─────┐ │         
│ │id│title │userId│ │         好处：灵活，适合快速迭代
│ ├──┼──────┼─────┤ │         代价：不太适合复杂关联查询
│ │1 │文章A │  1   │ │
│ └──┴──────┴─────┘ │
└─────────────────┘
```

**本项目选择 MongoDB 的原因**：
- 情绪记录结构不固定（有时加故事、有时加提醒日期）
- 用户收藏/点赞直接存数组，不用建关联表
- 适合快速迭代，改 Schema 成本低

**PM 该懂的——MongoDB 术语翻译**：

| MySQL 术语 | MongoDB 术语 | 含义 |
|------------|-------------|------|
| 数据库 Database | 数据库 Database | 本项目叫 `mood_memo2` |
| 表 Table | 集合 Collection | 如 `users`、`notes`、`treeholes` |
| 行 Row | 文档 Document | 一条用户记录、一条信纸 |
| 列 Column | 字段 Field | username、content 等属性 |
| 主键 Primary Key | `_id` (ObjectId) | MongoDB 自动生成的唯一ID |

### 4.2 本项目的5个集合

| 集合 | 存什么 | 关键索引 |
|------|--------|----------|
| `users` | 用户信息、收藏/点赞列表、阅读历史 | `username` 唯一索引 |
| `notes` | 心情信纸（便签） | `userId+createdAt`、`emotion`、`status` |
| `treeholes` | 回声广场帖子 | `status+createdAt`、`emotion` |
| `articles` | 文章 | `emotion+status`、`createdAt` |
| `comments` | 评论（树洞和文章共用一个集合） | `treeholeId+status`、`articleId+status` |

### 4.3 索引（Index）是什么——你必须懂的

**不用索引**：找一条数据要把整个集合翻一遍（全表扫描），数据多了就慢。

**用了索引**：像书的目录，MongoDB 直接跳到对应位置。

```
文章按 createdAt 查询（最新文章）：
  无索引 → 扫描10万条 → 2秒
  有索引 → 直接定位 → 几毫秒
```

**代价**：索引占存储空间，写入时需更新索引（略慢）。

**本项目已建索引的字段**（不用你操心，但你应该知道）：
- 所有 `createdAt` 字段（按时间排序查询频繁）
- `status` 字段（过滤已发布/待审核）
- `emotion` 字段（情绪筛选）
- 复合索引如 `{ status: 1, createdAt: -1 }`（"已发布 + 最新"一起查）

**PM 该问的**："这个新查询条件加了，对应的索引加了吗？会不会拖慢现有查询？"

### 4.4 数据迁移（Migration）

**这是什么**：项目改版时，旧数据的格式/字段值需要批量更新。

**本项目经历过的迁移**（`migrate-notes.js`）：
```
旧版 Note.status 值        →    新版 Note.status 值
────────────────────────────────────────────────
'日志'                     →    '思绪阁楼'
'悬置'                     →    '思绪阁楼'
'纪念'（开心/感动）         →    '留声匣'
'纪念'（其他）             →    '流星信箱'
```

**PM 该懂的**：
- 任何涉及改字段名、改枚举值的需求，都需要数据迁移方案
- 迁移脚本是一次性的，跑完就行
- 务必先备份数据库再跑迁移

**PM 该问的**："这个改动能向后兼容吗？存量数据需要跑迁移脚本吗？迁移脚本写好测试过了吗？"

---

## 五、前后端之间：它们怎么"说话"

### 5.1 HTTP 请求的本质

```
前端代码：
  axios.get('/api/articles?emotion=焦虑&page=1')
  
实际发出的 HTTP 请求：
  GET /api/articles?emotion=%E7%84%A6%E8%99%91&page=1 HTTP/1.1
  Host: localhost:3001
  Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
  Content-Type: application/json
  
后端返回：
  HTTP/1.1 200 OK
  Content-Type: application/json
  
  {
    "code": 200,
    "data": {
      "articles": [ ... ],
      "total": 42,
      "page": 1,
      "limit": 20
    }
  }
```

### 5.2 前后端协作流程（一个功能从提出到上线）

以"加一个文章搜索功能"为例：

```
1. PM提需求：用户在夜灯书房能按关键词搜索文章

2. 前后端沟通：
   前端："我需要调什么接口？参数是什么？返回什么格式？"
   后端："现有 GET /api/articles 接口，加 keyword 参数就行"
   前端："OK，那我在搜索框里 onEnter 发请求，keyword 拼在 query 里"

3. 后端改动：
   - routes/articles.js 里加 keyword 的处理逻辑（模糊匹配 title 和 summary）
   - 可能要加索引（title 和 summary 的文本索引）
   
4. 前端改动：
   - ArticlesPage.vue 加搜索框组件
   - 搜索触发后发 GET /api/articles?keyword=xxx
   - 结果替换当前列表
   
5. 联调测试：前后端都跑起来，实际测一遍
```

### 5.3 RESTful API 约定

本项目遵循的约定：

| 约定 | 示例 | 说明 |
|------|------|------|
| 资源名用复数 | `/api/articles` | 不是 `/api/article` |
| 嵌套资源 | `/treehole/:id/comments` | 树洞下的评论 |
| Query参数用于筛选 | `?emotion=焦虑&page=1` | 不是放在URL路径里 |
| 统一响应格式 | `{ code, message, data }` | 所有接口都一样 |

### 5.4 分页是怎么工作的

```
前端请求：GET /api/articles?page=2&limit=20

后端计算：
  skip = (page - 1) * limit = (2 - 1) * 20 = 20  ← 跳过前20条
  limit = 20                                      ← 取20条

MongoDB查询：
  Article.find(query).skip(20).limit(20)  ← 返回第21-40条

返回给前端：
  {
    data: { articles: [...20条...], total: 100, page: 2, limit: 20 }
  }
  前端算：还有 100 - 2*20 = 60 条没加载 → 显示"加载更多"按钮
```

**PM 该懂的**：讨论列表页时，分页大小（limit=20）是可以调的。调大=一页多显示但加载慢，调小=加载快但用户要多翻页。

---

## 六、安全与权限：谁能看什么、做什么

### 6.1 本项目的权限体系

```
角色层级：
游客（未登录）
  ├── 可浏览回声广场列表
  ├── 可阅读文章
  └── 不可发布/评论/点赞

注册用户（登录）
  ├── 游客的全部权限
  ├── 可写心事信纸
  ├── 可发布树洞
  ├── 可评论/点赞/收藏
  ├── 可查看成长归档室
  └── 可访问创作者中心

管理员
  ├── 注册用户的全部权限
  └── 可访问审核中心（审核所有UGC内容）
```

### 6.2 三层防线

```
第1层：前端显示层（可被绕过）
  - 未登录隐藏"发布"按钮
  - 非管理员隐藏"审核中心"菜单
  - 这是为了体验，不能依赖它保安全

第2层：路由守卫（router.beforeEach）
  - 未登录访问 /deposit → 跳转登录页
  - 非管理员访问 /admin → 跳转首页
  - 比第1层强，但理论上也可绕过

第3层：后端中间件（最终防线，无法绕过）
  - auth：检查JWT，没有就401
  - adminAuth：检查role，不是admin就403
  - 每个接口独立配置中间件
```

### 6.3 审核机制

```
用户发布内容 → status='待审核'
    ↓
管理员在审核中心看到
    ↓
┌─── 通过 → status='已发布' → 公开可见
└─── 驳回 → status='已驳回' → 仅作者可见，可重新编辑提交
```

**涉及审核的内容类型**：
- 树洞帖子（TREEHOLE）
- 树洞评论（COMMENT ON TREEHOLE）
- 用户投稿文章（ARTICLE SUBMISSION）
- 文章评论（COMMENT ON ARTICLE）

**审核通过时的副作用**：
- 树洞评论通过 → `treehole.commentCount + 1`
- 文章评论通过 → `article.commentCount + 1`

### 6.4 危机干预

后端在树洞发布时会检测以下关键词：
```
['自杀', '自残', '无助', '绝望', '不想活', '想死']
```

如果命中，返回 `{ crisis: true }`，前端可以展示危机干预热线或引导语。

### 6.5 登录安全（防暴力破解）

```
登录失败次数：
  第1次失败 → loginFailCount = 1
  第2次失败 → loginFailCount = 2
  ...
  第5次失败 → loginFailCount = 5 → lockUntil = 当前时间 + 15分钟
  
锁定期间：即使密码正确也返回"账号已锁定，请X分钟后重试"

登录成功 → loginFailCount 重置为0，lockUntil 清除
```

---

## 七、开发流程与工具链

### 7.1 项目是怎么"跑起来"的

```
┌─────────────────────────────────────────────┐
│                 开发环境                      │
│                                              │
│  Terminal 1: cd client && npm run dev        │
│  → Vite 启动开发服务器（端口5173）              │
│  → 热更新：改代码自动刷新浏览器                 │
│                                              │
│  Terminal 2: cd server && npm run dev        │
│  → nodemon 启动后端（端口3001）                │
│  → 改代码自动重启服务器                        │
│                                              │
│  Terminal 3: MongoDB（端口27017）              │
│  → 数据库服务需要先启动                        │
└─────────────────────────────────────────────┘
```

### 7.2 常用命令（你需要知道的）

| 命令 | 作用 |
|------|------|
| `npm install` | 安装项目依赖（第一次拉代码后执行） |
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 打包生产版本（部署用） |
| `node scripts/initData.js` | 初始化样例数据到数据库 |
| `node scripts/createAdmin.js` | 创建管理员账号 |
| `node src/migrate-notes.js` | 执行数据迁移 |

### 7.3 package.json 是什么

每个子项目（`client/` 和 `server/`）都有一个 `package.json`，它记录了三件事：

1. **项目名和版本**
2. **依赖列表**（`dependencies`）：项目用了哪些第三方库
3. **脚本命令**（`scripts`）：`npm run xxx` 实际执行什么

```
// server/package.json 简化版
{
  "name": "mood-post-office-server",
  "dependencies": {
    "express": "^4.x",       // Web框架
    "mongoose": "^x.x",      // MongoDB操作
    "jsonwebtoken": "^x.x",  // JWT认证
    "bcryptjs": "^x.x",      // 密码加密
    "cors": "^x.x",          // 跨域
    "dotenv": "^x.x"         // 环境变量
  },
  "scripts": {
    "dev": "nodemon src/index.js",   // 开发模式（自动重启）
    "start": "node src/index.js"     // 生产模式
  }
}
```

### 7.4 .env 环境变量

```
// server/.env
PORT=3001                              ← 后端端口号
MONGODB_URI=mongodb://localhost:27017/mood_memo2  ← 数据库地址
JWT_SECRET=mood_memo_secret_key_2024   ← JWT签名密钥（绝不能泄露）
JWT_EXPIRES_IN=7d                      ← Token有效期
```

**PM必须知道**：`.env` 文件包含敏感信息（密钥、密码），**绝对不能提交到Git仓库**，**不能发给外部人员**。

### 7.5 Git 版本控制（你需要懂的）

| 术语 | 白话 |
|------|------|
| **仓库 (Repo)** | 项目的Git存储空间 |
| **提交 (Commit)** | 一次代码变更的快照 |
| **分支 (Branch)** | 独立的开发线，不改动主分支 |
| **合并 (Merge)** | 把分支的改动合入主分支 |
| **Pull Request** | 申请把代码合并到主分支，需要审查 |
| **冲突 (Conflict)** | 两个人改了同一个文件的同一行，需人工解决 |

**推荐的开发分支策略**：
```
main（生产环境）
  └── develop（开发环境）
       ├── feature/xxx（新功能）
       └── fix/xxx（修Bug）
```

---

## 八、核心技术概念速查表

### 8.1 前端速查

| 术语 | 一句话解释 | 本项目位置 |
|------|-----------|------------|
| Vue 3 | 前端框架，用组件拼页面 | `client/src/` |
| Vite | 开发/打包工具 | `vite.config.js` |
| SPA | 单页应用，页面切换不刷新浏览器 | 整个 client |
| Router | URL → 页面组件的映射 | `router/index.js` |
| Pinia Store | 全局数据仓库 | `stores/index.js` |
| Axios | 发HTTP请求的库 | `stores/index.js` |
| SCSS | 带变量/嵌套的CSS | `main.scss` |
| v-if | "条件成立才显示" | 模板里的指令 |
| v-for | "循环渲染列表" | 模板里的指令 |
| v-model | "双向绑定"（输入框改→数据变，数据变→输入框变） | 表单绑定 |
| Props | 父组件给子组件传数据 | 组件间通信 |
| Emit | 子组件通知父组件 | 组件间通信 |

### 8.2 后端速查

| 术语 | 一句话解释 | 本项目位置 |
|------|-----------|------------|
| Express | Node.js Web框架 | `server/src/index.js` |
| Route | URL + HTTP方法 → 处理函数 | `routes/*.js` |
| Middleware | 请求处理前的"安检" | `middleware/auth.js` |
| Mongoose Schema | 数据结构的"蓝图" | `models/*.js` |
| JWT | 加密的"身份证"Token | `utils/helpers.js` |
| bcrypt | 密码不可逆加密 | `models/User.js` |
| .env | 环境变量（密钥/配置） | `server/.env` |

### 8.3 数据库速查

| 术语 | 一句话解释 |
|------|-----------|
| MongoDB | 文档型 NoSQL 数据库 |
| Collection | 类似 MySQL 的"表" |
| Document | 类似 MySQL 的"一行" |
| ObjectId | MongoDB 自动生成的唯一ID |
| Index | "目录"，加速查询 |
| Aggregate | 数据聚合/统计（如按情绪分组计数） |
| populate | 类似 JOIN（关联查询另一个集合的数据） |

### 8.4 通用术语速查

| 术语 | 一句话解释 |
|------|-----------|
| API | 前后端之间的约定接口 |
| JSON | 数据传输格式（`{"key": "value"}`） |
| HTTP | 前后端通信协议 |
| RESTful | API设计风格约定 |
| CORS | 浏览器跨域安全机制 |
| 限流 (Rate Limit) | 限制单位时间内的请求次数 |
| 分页 (Pagination) | 数据分批返回（page+limit） |
| 懒加载 (Lazy Load) | 用到才加载 |
| 热更新 (HMR) | 改代码自动刷新浏览器 |
| 部署 (Deploy) | 把代码放到服务器上运行 |

---

## 九、PM 必备话术：如何用技术语言描述需求

### 9.1 需求描述模板

当提一个新功能需求时，尝试用以下格式：

```markdown
## 需求：[一句话描述]

### 用户故事
- 作为[角色]，我想要[做什么]，以便[达到什么目的]

### 页面/入口
- 在[哪个页面]的[哪个位置]新增/修改[什么]

### 数据来源
- 数据从哪个接口获取？现有接口是否满足？
- 需要新增字段到哪个Model？

### 交互流程
- 用户点击A → 弹出B → 用户输入C → 点击确认 → 调接口 → 成功则D/失败则E

### 权限
- 游客/注册用户/管理员分别看到什么？

### 边界情况
- 数据为空时显示什么？
- 数据加载中显示什么？
- 操作失败显示什么？
```

### 9.2 你应该问开发的问题清单

**提需求前问自己**：
1. 这个数据现在有吗？（需要后端新建接口还是现有的够用？）
2. 动到数据库结构了吗？（需要数据迁移吗？）
3. 影响哪些已有功能？（改一个地方会不会搞崩另一个？）

**评审方案时问开发**：
1. "这个方案有考虑并发问题吗？"（两个人同时操作会不会出Bug？）
2. "存量数据兼容吗？需要跑迁移脚本吗？"
3. "加载状态的UI考虑了吗？网络慢的时候用户看到什么？"
4. "空数据状态设计了吗？列表为空显示什么？"
5. "错误处理做了吗？接口挂了用户看到什么？"
6. "这个字段的最大长度/最大数量是多少？超出怎么办？"

### 9.3 你应该避免说的（和不准确的说法）

| 不准确的说法 | 为什么不行 | 应该说 |
|-------------|-----------|--------|
| "数据库里加一列" | MongoDB 叫"字段(field)"不是"列(column)" | "Note Schema加个字段" |
| "点按钮的时候调接口" | "调接口"是对的，但要说明是GET还是POST | "POST /api/notes 创建一条" |
| "这个页面要实时刷新" | "实时"意味着 WebSocket，如果不必要 | "用户操作后重新请求列表" |
| "把数据缓存到本地" | "缓存"可能指浏览器缓存/Redis/Pinia | "存到 Pinia Store" 或 "存到 localStorage" |
| "后端传的数据不对" | 不够具体 | "GET /api/articles 返回的 articles 里缺少 category 字段" |

---

## 十、常见需求的技术代价评估

这是 PM 最有价值的能力之一：快速判断一个需求的技术成本。

### 10.1 改动成本级别

| 级别 | 改动范围 | 工时估算 | 示例 |
|------|---------|----------|------|
| 🟢 **极小** | 改前端文案/样式 | 0.5-2h | 改按钮颜色、改提示文案 |
| 🟢 **小** | 前端单页面逻辑 | 2-8h | 加个筛选tab、调整分页大小 |
| 🟡 **中** | 前后端都要改 | 1-3天 | 加一个新字段（Schema+接口+前端表单+展示） |
| 🟡 **中** | 新增接口+页面 | 2-5天 | 加一个新的列表页 |
| 🟠 **大** | 改Model+数据迁移 | 3-7天 | 改枚举值、改数据结构、存量数据迁移 |
| 🔴 **特大** | 涉及架构/权限/第三方 | 1-3周 | 加实时通知、接入第三方服务、重构权限 |

### 10.2 具体场景评估

| 需求 | 涉及改动 | 预估 |
|------|----------|------|
| "把文章列表每页20条改成10条" | 前端改 limit 参数 | 0.5h |
| "给便签加个'标签'字段" | Note Schema + 接口 + 前端表单 + 列表展示 | 1-2天 |
| "新增一个'我的草稿'页面" | 新页面 + 路由 + Note Schema加状态 + 接口筛选 | 2-3天 |
| "把评论从300字改成500字" | Note Schema + 前端表单 | 0.5h |
| "文章要有封面图" | 加封面图上传组件 + 后端存图片 | 2-4天 |
| "树洞要能发图片" | 加图片上传组件 + 后端存图片 + CDN | 3-5天 |
| "加实时通知（审核结果/新评论）" | WebSocket + 通知表 + 前端通知UI | 5-10天 |
| "加AI情绪分析" | 接入AI API + Prompt设计 + 结果展示 | 5-10天 |
| "把匿名码格式改成'城市+天气+数字'" | 改 `generateAnonymousCode()` | 0.5h |
| "首页心情分布改成折线图" | 前端替换图表库 | 1-2天 |

### 10.3 哪些需求"听起来简单但实际很复杂"

| 听起来 | 实际上 | 为什么 |
|--------|--------|--------|
| "加个撤销功能" | 需要操作历史栈 | 要记录每次操作，光标位置等 |
| "支持多人同时编辑" | 需要实时协同引擎 | 操作冲突解决巨复杂（参考Google Docs） |
| "所有地方都能拖拽排序" | 拖拽实现的坑很深 | 触摸/鼠标兼容、动画性能、数据同步 |
| "数据要实时同步" | WebSocket + 离线处理 | 断网重连、冲突合并、后端推送架构 |
| "做个App版本" | 几乎重写前端 | 即使跨端框架（Taro/RN），也有大量适配 |

---

## 附：本项目技术栈一览图

```
┌──────────────────────────────────────────────────────────┐
│                      心事邮局 技术栈                        │
├──────────────────────┬───────────────────────────────────┤
│                                      │                    │
│    前端 (client/)                   │   后端 (server/)    │
│    浏览器里运行                     │   Node.js 服务器    │
│                                      │                    │
│  ┌─────────────────┐                │  ┌──────────────┐  │
│  │    Vue 3         │ ← 框架         │  │  Express.js  │  │
│  ├─────────────────┤                │  ├──────────────┤  │
│  │  Vue Router      │ ← 页面跳转      │  │  JWT + bcrypt│  │
│  ├─────────────────┤                │  │  (认证+加密)  │  │
│  │  Pinia           │ ← 状态管理      │  ├──────────────┤  │
│  ├─────────────────┤                │  │  Mongoose     │  │
│  │  TipTap          │ ← 富文本编辑器   │  │  (MongoDB操作)│  │
│  ├─────────────────┤                │  └──────┬───────┘  │
│  │  Element Plus    │ ← UI组件库      │         │         │
│  ├─────────────────┤                │         │         │
│  │  SCSS            │ ← 样式          │  ┌──────▼───────┐  │
│  ├─────────────────┤                │  │   MongoDB     │  │
│  │  Axios           │ ← HTTP请求     │  │  mood_memo2   │  │
│  └─────────────────┘                │  └──────────────┘  │
│                                      │                    │
│  构建: Vite                          │  环境变量: .env     │
│  包管理: npm                         │  端口: 3001         │
└──────────────────────────────────────┴────────────────────┘
```

---

> **最后的建议**：你不必一次记住所有内容。把这份文档当作"字典"——开发提到一个你不懂的词，回来查一下就行。跟开发团队多在一起坐，听他们讨论，三周后你会发现大部分术语都能听懂了。